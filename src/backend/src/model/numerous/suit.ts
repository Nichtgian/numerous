export class Suit {
    code: string;
    title: string;
    ascii: string;

    constructor(code, title, ascii) {
        this.code = code;
        this.title = title;
        this.ascii = ascii;
    }
}